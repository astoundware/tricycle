import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 5,
    borderWidth: 1,
    borderColor: '#cccccc',
    paddingHorizontal: 12,
    paddingVertical: 5,
    alignItems: 'center',
  },
  image: {
    width: 16,
    height: 16,
  },
});
